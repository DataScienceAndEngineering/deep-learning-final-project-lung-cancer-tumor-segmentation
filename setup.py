from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project is aim to create is to create a classification model when viewing a chest CT scan that can detect a tumor or not.',
    author='lung-cancer-tumor-segmentation-team',
    license='',
)
